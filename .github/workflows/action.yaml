name: home_work_26
on: [push]
jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: list dir
        run: ls -la
        uses: actions/checkout@v2
      - name: list dir 2
        run: ls -la
